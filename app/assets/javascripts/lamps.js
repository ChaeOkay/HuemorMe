$(document).ready(function(){
  $('.slider-input').slider({
    min: 0,
    max: 100,
    step: 1,
    slide: function(event, ui){
      //change value of f.hidden_field brightnes value
    }
  })
})
