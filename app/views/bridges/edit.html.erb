<div class="row">
	<div class="large-10 large-centered columns text-center">

		<% if flash[:notice] %>
		  <p><%= flash[:notice] %></p>
		<% end %>

		"Push the button"

		<%= form_for [current_user, @bridge] do |f| %>
		  <%= f.submit "Update Your Bridge IP" %>
		<% end %>

	</div>
</div>