<%= form_for([lamp.bridge, lamp], url: bridge_lamp_path(lamp.bridge, lamp))  do |f| %>
  <%= f.hidden_field :command, value: "toggle_on_off" %>
  <%= f.submit "Toggle #{lamp.say_on_off}?"%>
<% end %>

<%= form_for([lamp.bridge, lamp], url: bridge_lamp_path(lamp.bridge, lamp))  do |f| %>
  <%= f.hidden_field :command, value: "toggle_colorloop" %>
  <%= f.submit "Toggle Colorloop #{lamp.say_colorloop}?"%>
<% end %>
