<div class="background">

  <div class="row">
    <div class="large-6 columns left">
      <div class="font-white tagline">
        <div class="splash-body">
          Access your Philips Hue LED light bulbs from anywhere in the world.
        </div>
        <div class="splash-body">
          Program the lighting conditions from our web app to control your lights.
        </div>
        <div class="splash-body">
          Signup to install a custom daemon on your local network, which runs on a Raspberry Pi.*
        </div>
        <div class="splash-ast">
          * Or whatever Unix-y system you want.
        </div>
      </div>
    </div>

      <div class="form-background">
        <div class="form-offset">
          <%= render '/layouts/login_form' %>
          <h1 class="font-white">Sign Up</h1>
          <%= render "new_form" %>
        </div>
      </div>
    </div>

</div>
